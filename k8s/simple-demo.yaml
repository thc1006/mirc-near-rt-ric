apiVersion: apps/v1
kind: Deployment
metadata:
  name: oran-dashboard
  namespace: oran-nearrt-ric
  labels:
    app: oran-dashboard
    component: dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oran-dashboard
  template:
    metadata:
      labels:
        app: oran-dashboard
        component: dashboard
    spec:
      containers:
      - name: dashboard
        image: nginx:alpine
        ports:
        - containerPort: 80
          name: http
        volumeMounts:
        - name: dashboard-content
          mountPath: /usr/share/nginx/html
      volumes:
      - name: dashboard-content
        configMap:
          name: oran-dashboard-content
---
apiVersion: v1
kind: Service
metadata:
  name: oran-dashboard
  namespace: oran-nearrt-ric
  labels:
    app: oran-dashboard
    component: dashboard
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    name: http
  selector:
    app: oran-dashboard
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: oran-dashboard-content
  namespace: oran-nearrt-ric
data:
  index.html:  < /dev/null | 
    <!DOCTYPE html>
    <html>
    <head>
        <title>O-RAN Near-RT RIC Platform</title>
        <style>
            body { font-family: Arial, sans-serif; margin: 40px; background: #f5f5f5; }
            .container { max-width: 1200px; margin: 0 auto; background: white; padding: 30px; border-radius: 8px; }
            .header { text-align: center; margin-bottom: 40px; }
            .status-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 20px; }
            .status-card { background: #f8f9fa; padding: 20px; border-radius: 8px; border-left: 4px solid #28a745; }
            .component-name { font-size: 18px; font-weight: bold; color: #333; margin-bottom: 10px; }
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1>🚀 O-RAN Near-RT RIC Platform</h1>
                <p><strong>Status:</strong> <span style="color: #28a745;">✅ OPERATIONAL</span></p>
            </div>
            <div class="status-grid">
                <div class="status-card">
                    <div class="component-name">📡 E2 Interface</div>
                    <div>E2 Termination Deployed</div>
                </div>
                <div class="status-card">
                    <div class="component-name">🤖 FL Coordinator</div>
                    <div>Federated Learning Active</div>
                </div>
                <div class="status-card">
                    <div class="component-name">📊 xApp Dashboard</div>
                    <div>Application Management Ready</div>
                </div>
            </div>
            <h3>🎉 O-RAN Platform Successfully Deployed!</h3>
        </div>
    </body>
    </html>
